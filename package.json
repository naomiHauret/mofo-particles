{
  "name": "particules",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^0.28.7",
    "html-webpack-plugin": "^2.30.1",
    "style-loader": "^0.18.2",
    "uglifyjs-webpack-plugin": "^1.0.0-beta.2",
    "webpack": "^3.6.0",
    "webpack-dev-server": "^2.8.2"
  },
  "scripts": {
    "start": "npm run build && webpack-dev-server",
    "build": "NODE_ENV=production webpack",
    "deploy": "npm run build && git add dist/ && git push && git subtree push --prefix dist origin gh-pages"
  }
}
